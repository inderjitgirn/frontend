pipeline {
        dockerfile {
      filename 'Dockerfile.dev'
      additionalBuildArgs  '--build-arg inderjitgirn\frontend'
    }
    stages{
      stages("test"){
        steps{
          echo "hi there"
        }
      }
    }
}
